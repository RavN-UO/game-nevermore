# Optional: online global leaderboard via Supabase (free tier).
# Without these, the game uses the local leaderboard only and stays fully
# playable offline. See src/systems/Leaderboard.ts for the table SQL.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
